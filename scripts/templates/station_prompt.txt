あなたは不動産売買仲介業者が使用するWebサイトのバックエンドデータ作成の専門家です。
以下の要件に100%従い、戸建ての購入希望者データを生成してください。

# 実行パラメータ
MODE: "station"
TARGET_RAIL_COMPANY: "{{RAIL}}"
TARGET_LINE: "{{LINE}}"
TARGET_STATION: "{{STATION}}"

# 【重要】詳細情報非表示戦略
- 購入希望者の詳細情報（価格、希望条件、家族構成、年齢、理由など）は一切記載しない
- 戦略：「購入希望者情報の詳細はお問い合わせください」として、問い合わせ数を最大化するため
- 表示するのは「物件タイプ」「エリア/駅」「購入希望時期」のみ

# 混在禁止（最重要）
- mode="station" のとき：rail_company/line/station だけ具体値固定、area/city は必ず null（具体値禁止）

# 出力要件
- 件数：16〜63件
- 出力：const kodate_db = [ ... ];
- 説明文/Markdown/コードフェンスは禁止（出力は上記のみ）

# スキーマ（必須キーのみ・詳細情報は含めない）
id,type,scope,area,city,rail_company,line,station,timing

**注意：price, walk_time, family, occupation, age, land_area, building_area, parking, built_year, reason, ng は含めないこと**

# 固定/候補
- type="戸建て"
- scope="station"
- rail_company="{{RAIL}}"（全件固定）
- line="{{LINE}}"（全件固定）
- station="{{STATION}}"（全件固定）
- area=null, city=null（全件固定）
- timing: "即入居希望","3ヶ月以内","半年以内","1年以内","時期未定"

# 参照一覧（この中からのみ選べ：表記揺れ禁止）
{{ALLOWED_STATION_LIST_FOR_THIS_LINE_ONLY}}

# 出力書式
- **必須：完全なJSON形式で出力すること（キーにも必ずダブルクォートを付ける）**
- 例：{"id": "...", "type": "戸建て", ...}（正しい）
- 例：{id: "...", type: "戸建て", ...}（間違い・禁止）
- 文字列はダブルクォート、nullはnull
- 詳細情報キー（price, walk_time, family, occupation, age, land_area, building_area, parking, built_year, reason, ng）は絶対に含めないこと
